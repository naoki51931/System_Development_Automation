import{ApiError}from"@/lib/api";export function AsyncState({loading,error,empty=false}:{loading:boolean;error:unknown;empty?:boolean}){if(loading)return <p aria-live="polite" role="status">読み込み中…</p>;if(error){const e=error as ApiError;return <div role="alert" className="error"><strong>{e.status||500}</strong> {e.message}</div>}if(empty)return <p className="card">データはありません。</p>;return null}
