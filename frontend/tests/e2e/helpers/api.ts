import type{APIRequestContext}from"@playwright/test";const BASE=process.env.E2E_API_BASE||"http://localhost:8000/api/v1";export async function login(request:APIRequestContext,role:string){const users=await(await request.get(`${BASE}/auth/local/users`)).json();const user=users.find((x:{email:string})=>x.email===`${role}@quality.local`);if(!user)throw new Error(`quality seed missing ${role}`);const response=await request.post(`${BASE}/auth/local/login`,{data:{user_id:user.id}});const body=await response.json();const me=await(await request.get(`${BASE}/auth/me`)).json();return{csrf:body.csrf_token,user,org:me.organizations[0]}}
export async function post(request:APIRequestContext,path:string,csrf:string,data:unknown){return request.post(`${BASE}${path}`,{headers:{"X-CSRF-Token":csrf},data})}
export{BASE};
