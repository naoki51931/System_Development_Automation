import AxeBuilder from"@axe-core/playwright";import{test,expect}from"@playwright/test";for(const path of ["/login","/","/projects","/estimates","/reviews","/chat","/users"]){test(`no serious accessibility violations ${path}`,async({page})=>{await page.goto(path);const result=await new AxeBuilder({page}).analyze();expect(result.violations.filter(x=>["critical","serious"].includes(x.impact||""))).toEqual([])})}
