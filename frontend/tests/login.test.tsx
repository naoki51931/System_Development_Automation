import React from"react";import{render,screen}from"@testing-library/react";import{describe,expect,it,vi}from"vitest";vi.mock("next/navigation",()=>({useRouter:()=>({push:vi.fn()})}));vi.mock("../lib/api",()=>({api:vi.fn().mockResolvedValue([])}));import Login from"../app/login/page";describe("LocalAuth login",()=>{it("テストユーザー選択を表示する",()=>{render(<Login/>);expect(screen.getByLabelText("テストユーザー")).toBeInTheDocument();expect(screen.getByRole("button",{name:"ログイン"})).toBeDisabled()})});
