import{afterEach,describe,expect,it,vi}from"vitest";import{api,ApiError}from"../lib/api";afterEach(()=>vi.restoreAllMocks());describe("API client",()=>{it("409を競合メッセージへ変換する",async()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:false,status:409,json:async()=>({})}));await expect(api("/x")).rejects.toMatchObject({code:"VERSION_CONFLICT",message:"別のユーザーにより更新されました。最新情報を読み込み直してください。"})});it("危険なHTMLをDOMへ挿入しない",()=>{const value='<img src=x onerror="alert(1)">';const node=document.createElement("p");node.textContent=value;expect(node.querySelector("img")).toBeNull()})});
