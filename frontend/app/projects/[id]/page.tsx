"use client";import {use,useEffect,useState}from"react";import{Shell}from"@/components/Shell";import{api}from"@/lib/api";export default function Page({params}:{params:Promise<{id:string}>}){const{id}=use(params);const[p,setP]=useState<Record<string,string>>({});useEffect(()=>{api<Record<string,string>>(`/projects/${id}`).then(setP)},[id]);return <Shell title="案件詳細"><section className="card"><h2>{p.name||"読み込み中…"}</h2><dl><dt>案件コード</dt><dd>{p.project_code}</dd><dt>状態・工程</dt><dd>{p.status} / {p.current_phase}</dd></dl><p>見積・契約・支払い・成果物・レビュー・チャット・修正依頼・保守を案件単位で管理します。</p></section></Shell>}
