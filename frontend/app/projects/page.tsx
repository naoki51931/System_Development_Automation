"use client"; import Link from "next/link";import {useEffect,useState} from "react";import {Shell} from "@/components/Shell";import {api} from "@/lib/api";type Page<T>={items:T[];next_cursor:string|null};type Project={id:string;project_code:string;name:string;status:string;current_phase:string};export default function Page(){const [rows,setRows]=useState<Project[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{const org=localStorage.getItem("organization_id")||"";api<Page<Project>>(`/projects?organization_id=${encodeURIComponent(org)}`).then(x=>setRows(x.items)).catch(()=>setError("案件を取得できませんでした")).finally(()=>setLoading(false))},[]);return <Shell title="案件一覧">{loading?<p aria-live="polite">読み込み中…</p>:error?<div role="alert" className="error">{error}</div>:<table><thead><tr><th>案件コード</th><th>案件名</th><th>状態</th><th>現在工程</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td><Link href={`/projects/${x.id}`}>{x.project_code}</Link></td><td>{x.name}</td><td>{x.status}</td><td>{x.current_phase}</td></tr>)}</tbody></table>}</Shell>}
